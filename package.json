{
  "name": "jira-actions",
  "packageManager": "yarn@3.1.1",
  "scripts": {
    "build": "esbuild src/**/*.ts --outdir=dist --outbase=src --minify --format=cjs --platform=node --bundle",
    "dev": "yarn build --watch"
  },
  "dependencies": {
    "@actions/core": "^1.6.0",
    "dayjs": "^1.10.7",
    "got": "^12.1.0",
    "zx": "^4.2.0"
  },
  "devDependencies": {
    "dprint": "^0.21.2",
    "esbuild": "^0.14.2",
    "husky": "^7.0.4",
    "typescript": "^4.5.2"
  }
}
